\section{12. BFS}

\subsection{Домашнее задание}
\begin{enumerate}
  \item
    Дан взвешенный орграф с положительными весами и вершина $s$.
    Нужно для каждой вершины $v$ найти число кратчайших путей из $s$ в $v$ по модулю $p$. $\O(E \log V)$.

    Решение:

    \begin{itemize}
      \item Модифицируем алгоритм Дейкстры -- в момент сравнивания расстояний, 
      мы так же будем пересчитывать количество кратчайших путей.

      \item Доказательство -- рассмотрим один шаг Дейкстры:
      
      Пусть $count[v]$ -- количество кратчайших путей до вершины $v$
      
      Обрабатываем вершину $vertex$ и её соседа $neighbor$ с весом $weight$.
      Тогда длина нового пути, проходящего через $vertex$ до $neighbor$, равна $new\_dist$, а длина наиболее короткого на данный момент пути -- $dist[neighbor]$

      Рассмотрим возможные случаи сравнения:
      
      \begin{enumerate}
        \item $new\_dist > dist[neighbor]$: нашли более длинный путь, поэтому пропускаем его

        \item $new\_dist < dist[neighbor]$: нашли более короткий путь.
        Все предыдущие пути до $neighbor$ становятся неоптимальными, поэтому $count[neighbor] := count[vertex]$

        \item $new\_dist = dist[neighbor]$: нашли ещё один кратчайший путь той же длины.
        
        Тогда $count[neighbor] := count[neighbor] + count[vertex]$ -- добавляем новые способы добраться до $neighbor$.
      \end{enumerate}

      \item Алгоритм:

      \begin{lstlisting}[language=Python]
import heapq

def dijkstra(graph, start, p):
    dist = [float('inf')] * len(graph)
    dist[start] = 0

    count = [0] * len(graph)
    count[start] = 1

    heap = [(0, start)]

    while heap:
        curr_dist, vertex = heapq.heappop(heap)
        
        if curr_dist > dist[vertex]:
            continue
            
        for neighbor, weight in graph[vertex]:
            new_dist = curr_dist + weight

            if new_dist < dist[neighbor]:
                dist[neighbor] = new_dist
                count[neighbor] = count[vertex]
                heapq.heappush(heap, (new_dist, neighbor))

            elif new_dist == dist[neighbor]:
                count[neighbor] = (count[neighbor] + count[vertex]) % p
    
    return count

    \end{lstlisting}

    \item Сложность по времени: равна временной сложности алгоритма Дейкстры с бинарной кучей -- $\O(|E|\log|V|)$
    \item Сложность по памяти: $\O(|V|)$ -- маасивы $dist$ и $count$

    \end{itemize}

  \item
    В орграфе есть бесплатные и $K$ типов платных ребер. Передвижение
    по любому ребру стоит $0$, но, чтобы двигаться по платному ребру
    типа $x$, необходимо иметь пропуск того же типа. В любой момент
    времени можно иметь не более одного пропуска, однако в любой
    вершине есть возможность купить пропуск любого типа за $A$ и
    продать пропуск любого типа за $B$ ($0 < B < A$, цены одинаковы
    во всех вершинах). Найдите самый дешевый способ добраться из вершины
    $s$ до вершины $t$ за $\O(K (V + E))$.

    Решение:

  \item
    Дан ориентированный граф с выделенными вершинами $s$ и $t$. Перемещение по любому ребру занимает 1 единицу времени.
    В начальный момент времени мы стоим в вершине $s$, и во все ребра с постоянной скоростью $L$ начинает поступать вода.
    У каждого ребра $e$ есть емкость $C_e$ --- минимальное количество воды в этом ребре, при котором по нему больше
    нельзя двигаться (мы должны закончить движение по ребру не позднее момента достижения его емкости). Найдите
    максимальное $L$, при котором все еще существует возможность добраться от $s$ до $t$ за $\O((V + E)\log{C_{\max}})$.

    Решение:

  \item
    Постройте матрицу $n \times n$, состоящую из клеток-стенок и пустых клеток,
    на которой при запуске \texttt{BFS} из какой-то клетки максимальный
    размер очереди будет $\omega(n)$. Ходить можно между клетками, смежными по стороне.

    Решение:

\end{enumerate}
